# @duzh/assets

Contains all game assets, including JSON data for units, items, etc. as well
as all game images.

Also contains scripts for packaging asset and image bundles.  We use Sharp for
compressing PNG images, and store them as Base64 data URLs in a JSON object.

