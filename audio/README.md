# @duzh/audio

Contains helpers for audio playback using the Web Audio API.
Game audio is fully synthesized using `Oscillator`s, and consists
entirely of square waves played at specific frequencies and durations.

`SoundPlayer` - used for playing `SoundEffect`s
`MusicPlayer` - used for playing `MusicModel`s and `Suite`s

