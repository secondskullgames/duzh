{
  "name": "robed_wizard",
  "offsets": {
    "dx": -4,
    "dy": -20
  },
  "pattern": "${sprite}_${activity}_${direction}_${number}",
  "animations": {
    "standing": {
      "pattern": "${sprite}_${direction}_${number}",
      "frames": [{
        "activity": "standing",
        "number": "1"
      }]
    },
    "walking": {
      "pattern": "${sprite}_${direction}_${number}",
      "frames": [{
        "activity": "walking",
        "number": "1"
      }]
    },
    "attacking": {
      "pattern": "${sprite}_${direction}_${number}",
      "frames": [{
        "activity": "attacking",
        "number": "1"
      }]
    },
    "damaged": {
      "pattern": "${sprite}_${activity}_SE_${number}",
      "frames": [{
        "activity": "stunned",
        "number": "1"
      }]
    }
  }
}
